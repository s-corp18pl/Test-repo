name: Documentation Configuration Changed

on:
  push:
    branches:
      - gh-pages
    paths:
      - 'Doxyfile'
      - 'static/**'

jobs:
  validate-data:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Documentation configuration files have been changed."